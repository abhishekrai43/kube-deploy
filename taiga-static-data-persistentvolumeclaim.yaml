apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: taiga-static-data
  name: taiga-static-data
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  volumeName: taiga-static-vol
  resources:
    requests:
      storage: 100Gi
status: {}
